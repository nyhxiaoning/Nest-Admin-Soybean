# PostgreSQL 连接（Prisma CLI 需要此文件，与 .env.development 保持一致）
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/nest_admin?schema=public&sslmode=disable

# 数据库 SSL 配置（必须与 .env.development 保持一致,TODO:大坑，否则默认给true）
DB_SSL=false
