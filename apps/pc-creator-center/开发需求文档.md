## 1.登录模块

我想要增加一个登录的模块功能：支持手机号验证码和账号密码登录功能，当前的接口文件实现如下：
apps/pc-creator-center/src/api/auth.ts
请帮我参考这个文件定义，增加后端nestjs的单独的登录模块实现（注意：这个登录模块和nestjs/server原有的登录模块不一样，），请实现新的登录模块设计，数据库使用prisma，验证码使用redis，默认返回四位数的验证码，给出设计和实现，我的server端口具体位置：apps/server

- 开发文档：[[docs/plans/2026-08-19-pc-creator-center-auth-design.md]]

### 我需要明白一下，增加primsa数据库后，这里的数据表的初始化和生成迁移的全流程。

#### 1.这里生成了数据库的迁移文件：
- 增加模块说明：apps/server/prisma/schema.prisma
- 增加当前的迁移sql表：apps/server/prisma/migrations/202608190001_add_pc_creator_center_auth/migration.sql


#### 2.开发：确认目标数据库后，可在 apps/server 执行：
pnpm prisma:deploy

但是开发环境执行：
pnpm prisma:migrate

#### 3.执行报错：pnpm prisma:migrate

pnpm prisma:migrate
 ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL  Command "prisma:migrate"not found

Did you mean "pnpm esvalidate"?
❯ npm prisma:migrate

```
pnpm prisma migrate dev # 开发环境
# 开发环境迁移（交互式）
npx prisma migrate dev

# 或生产环境部署
pnpm prisma migrate deploy # 生产环境

# 或查看状态
pnpm prisma migrate status 

```

#### 4.开发文档记录：开发环境完成开发后，如何线上部署prisma
