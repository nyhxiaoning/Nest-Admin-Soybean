# 安装使用地址：

注意用下面的安装方式：

```
brew install dbmate
dbmate --help

```


下面不能直接安装，是二进制
- [安装地址](https://github.com/amacneil/dbmate/releases/tag/v2.35.1)

- [代码地址](https://github.com/amacneil/dbmate)


# 先说结论

**如果你只有一台数据库、单人开发、永远不需要回滚、没有多环境、没有团队协作，那确实完全可以直接手动执行 SQL，不需要 dbmate。**

但是一旦你的项目进入：**多人开发、多环境（本地‑测试‑生产）、迭代更新表结构、需要回滚变更**，纯手动执行 SQL 就会出现大量很难排查的故障。 dbmate 就是用来解决这一系列痛点的工具，哪怕你从头到尾只用 Postgres，不切换 MySQL，它也有价值。

## 一、纯手动执行 SQL 的典型问题（不用迁移工具的坑）

### 1、你没办法记住：哪一条脚本已经在生产库跑过了

举个例子： 第1次上线，你手动跑了 `create_user.sql`。 过两周迭代，新增字段，你又写了 `add_age_column.sql`。

> 问题：半年之后，你已经记不清：
>
> - 测试库有没有执行 add_age_column？
> - 生产库有没有执行？ 如果再次执行一遍 `add_age_column.sql`，Postgres 就会报错「字段已存在」。

dbmate 在你的数据库里自动维护一张表 `schema_migrations`，记录**已经执行完毕的迁移版本号**。 下次执行 `dbmate up`，它只会运行**还没执行过**的脚本，不会重复执行旧脚本。

### 2、多个人开发数据库，很容易环境不一致

团队两个人：

- 开发者A 本地跑了新增索引脚本
- 开发者B 并不知道这条变更，他本地数据库缺少索引

没有统一版本记录，两个人本地库结构慢慢分叉，最后代码在别人电脑上报错。 所有人统一用 `dbmate up`，所有人数据库结构就会对齐。

### 3、上线部署流程混乱，容易漏脚本

部署生产的时候，你要手动列出一份清单：

> 需要依次执行脚本1、脚本2、脚本3……

一旦你漏掉其中一条 SQL，生产数据库结构就和开发环境不一致，线上出现诡异Bug。 使用 dbmate，CI/CD 流水线只需要一条命令：`dbmate up`，自动按时间顺序执行全部未运行脚本。

### 4、想要撤销改动（回滚）非常麻烦

假设你上线一条加字段的脚本，结果线上出问题，需要立刻删掉这个字段。 手动方案：你得自己再手写一条 `ALTER TABLE drop column` 的回滚SQL，并且你得记住这条回滚脚本什么时候执行。

dbmate 规范要求你可以编写向下回滚脚本，一条命令 `dbmate rollback` 撤销上一次变更。

> ⚠️ dbmate 的回滚脚本也是你手写的，它不会自动生成。但是它**管理回滚的执行顺序**。

### 5、SQL 和代码版本脱节

如果你把建表SQL随手存在电脑桌面，没有提交到 Git。 过一段时间换一台服务器部署，你找不到历史变更脚本。

dbmate 的迁移脚本全部放在项目目录 `db/migrations`，跟着代码一起提交 Git。 数据库变更脚本和后端代码**永远绑定在一起，同一个 Git 版本对应一套数据库结构**。

------

## 二、哪怕你全程只用 Postgres，dbmate 给你的核心价值

1. **幂等执行保障**：脚本只会跑一次，不会重复执行
2. **变更版本追踪**：`schema_migrations` 记录表，数据库知道自己当前处于哪一个版本
3. **统一执行入口**：本地、测试机、生产服务器、CI流水线全部用一条 `dbmate up`
4. **Git 绑定管理**：数据库结构变更和业务代码一同提交、评审、发布
5. **标准化回滚流程**
6. **环境对齐**：所有开发人员、所有服务器数据库结构保持一致

> 👉 前面提到的「一份工程可以跑多种数据库」只是 dbmate 的附加能力，**并不是它存在的核心价值**。 **绝大多数人使用 dbmate，从头到尾就只用 Postgres / MySQL 单一数据库。**

------

## 三、什么时候你完全不需要 dbmate？

满足下面全部条件，可以直接手动执行 SQL，不用迁移工具：

1. 单人项目，没有团队协作
2. 数据库一次性建好表，后续几乎不再改动表结构
3. 只有一个数据库环境，没有测试库、生产库区分
4. 不需要自动化部署流水线，每次上线都可以远程登录数据库手动运行SQL

------

## 四、两种工作方式对比

| 对比项                | 手动执行Postgres SQL       | 使用 dbmate                       |
| --------------------- | -------------------------- | --------------------------------- |
| 执行判断              | 人脑记住哪些脚本已经运行过 | 数据库schema_migrations表自动记录 |
| 多环境一致性          | 容易漏脚本、环境分叉       | 一条命令所有环境自动对齐          |
| 团队协作              | 容易出现本地库不一致       | 所有人执行相同迁移脚本            |
| 部署流水线            | 无法自动化，需要人工干预   | CI/CD 一键迁移                    |
| 回滚变更              | 手动编写并且记住回滚脚本   | 标准化 rollback 命令              |
| 切换MySQL等其他数据库 | 完全不支持                 | 工程结构可以复用（SQL需适配）     |

------

# 最简总结

dbmate 的本职工作**不是用来把 Postgres 迁移到 MySQL**。 它最主要的使命：**管理数据库迭代变更的生命周期，保证开发‑测试‑生产环境数据库版本一致，实现自动化部署**。

如果你后续想，我可以给你一份「单Postgres项目，从零搭建dbmate迁移」最小示例。
