# 开发文档记录


## 文学编辑室｜初版精简菜单 & 最小可用功能需求设计思路

初版砍掉复杂次级菜单、高级素材、版本回溯、批量导出等后期功能；只保留**写稿‑管理‑素材‑设置**四大核心，优先保证基础写作闭环，后续迭代再扩展。

适合 MVP 第一期开发，降低开发量、快速跑通业务。

## 一、一级侧边菜单结构（精简版，无多层嵌套子菜单）


```
文学编辑室
1. 文稿工作台（首页）
2. 我的稿件
3. 常用素材库
4. 标签管理
5. 编辑室设置
```

### 每个菜单详细初版功能

### 1、文稿工作台（首页，核心入口）

定位：快速创作 + 最近文稿看板

1. 一键新建 Markdown 文稿
2. 最近编辑列表（最近 10 篇稿件快捷进入）
3. 草稿快捷保存、自动保存
4. 基础文稿信息：标题、字数、创建时间

> 编辑器基础能力：markdown 编辑、实时预览、基础字体、换行、图片粘贴上传

### 2、我的稿件

整合原先全部文稿、草稿、已发布、回收站，采用页面内标签页切换，不再做成嵌套菜单

Tab 选项卡：

- 草稿箱

- 正式稿件

- 归档稿件

- 回收站（恢复 / 永久删除）

   

基础操作：重命名、复制、删除、导出单篇 MD/PDF

### 3、常用素材库（轻量化）

初版只保留高频文本素材，图片、音频素材二期再做

1. 自定义短句、金句、典故收藏
2. 素材新增、编辑、删除、搜索
3. 编辑文稿时可以快捷插入素材

### 4、标签管理

1. 自定义文稿标签（随笔、散文、小说、文案等）
2. 给稿件绑定标签、按标签筛选文稿

### 5、编辑室设置

1. 编辑器偏好：字体、字号、自动保存开关
2. 默认导出格式配置
3. 存储空间查看

---

## ✅ 文学编辑室模块｜初版实现完成记录

> **更新时间**: 2026-08-08
> **分支**: `feature/add-twomenu`
> **状态**: ✅ 已完成（初版 MVP，可独立跑通基础写作闭环）

### 设计意图（回顾）

初版砍掉复杂次级菜单、高级素材、版本回溯、批量导出等后期功能；只保留**写稿‑管理‑素材‑设置**四大核心，优先保证基础写作闭环，后续迭代再扩展。

---

## 一、一级菜单结构（已实现）

```
文学编辑室
├── 文稿工作台（首页）
├── 我的稿件
├── 常用素材库
├── 标签管理
├── 编辑室设置
└── 打印预览（路由独立页面，非菜单项）
```

---

## 二、后端实现（NestJS）

### 2.1 模块结构

| 子模块 | 文件路径 |
|---|---|
| 模块入口 | `apps/server/src/module/literature/literature.module.ts` |
| 常量定义 | `apps/server/src/module/literature/literature.constant.ts` |
| 文稿工作台 | `apps/server/src/module/literature/workbench/` |
| 文稿管理 | `apps/server/src/module/literature/manuscript/` |
| 素材管理 | `apps/server/src/module/literature/material/` |
| 标签管理 | `apps/server/src/module/literature/tag/` |
| 编辑室设置 | `apps/server/src/module/literature/setting/` |
| 图片上传 | `apps/server/src/module/literature/upload/` |

### 2.2 接口清单

#### 工作台 `GET /literature/workbench/overview`
返回统计卡片数据：草稿数、正式稿件数、归档稿件数、总字数、素材数、标签数，以及最近 10 篇文稿列表。

#### 文稿管理（全量 13 个接口）

| 方法 | 接口 | 说明 |
|---|---|---|
| GET | `/literature/manuscript/list` | 分页列表（按状态/标签/关键词筛选） |
| GET | `/literature/manuscript/recent` | 最近 10 篇（工作台专用） |
| GET | `/literature/manuscript/:id` | 详情（含内容与标签） |
| POST | `/literature/manuscript` | 新建文稿（草稿） |
| PUT | `/literature/manuscript` | 更新文稿 |
| PUT | `/literature/manuscript/save` | 自动保存（轻量，不触发布尔） |
| PUT | `/literature/manuscript/status` | 修改状态（发布/归档/回收站） |
| POST | `/literature/manuscript/copy/:id` | 复制为草稿副本 |
| PUT | `/literature/manuscript/tags` | 绑定/替换标签 |
| DELETE | `/literature/manuscript/:id` | 移入回收站（软删除） |
| PUT | `/literature/manuscript/restore/:id` | 从回收站恢复为草稿 |
| DELETE | `/literature/manuscript/permanent/:id` | 永久删除 |

#### 素材管理（全量 5 个接口）

| 方法 | 接口 | 说明 |
|---|---|---|
| GET | `/literature/material/list` | 分页列表（按类型/关键词筛选） |
| GET | `/literature/material/:id` | 详情 |
| POST | `/literature/material` | 新建素材 |
| PUT | `/literature/material` | 更新素材 |
| DELETE | `/literature/material/:id` | 删除（软删除） |

#### 标签管理（全量 5 个接口）

| 方法 | 接口 | 说明 |
|---|---|---|
| GET | `/literature/tag/list` | 分页列表（含关联文稿数） |
| GET | `/literature/tag/:id` | 详情 |
| POST | `/literature/tag` | 新建标签 |
| PUT | `/literature/tag` | 更新标签 |
| DELETE | `/literature/tag/:id` | 删除标签（同时解除关联） |

#### 编辑室设置（全量 2 个接口）

| 方法 | 接口 | 说明 |
|---|---|---|
| GET | `/literature/setting` | 获取当前用户设置（首次自动创建默认值） |
| PUT | `/literature/setting` | 更新设置 |

#### 图片上传（免权限，登录即可）

| 方法 | 接口 | 说明 |
|---|---|---|
| POST | `/literature/upload` | Markdown 编辑器图片粘贴上传（内存存储，单文件最大 10 MB） |

### 2.3 常量与状态码

```typescript
// 文稿状态
ManuscriptStatus.DRAFT      = '0'  // 草稿箱
ManuscriptStatus.PUBLISHED  = '1'  // 正式稿件
ManuscriptStatus.ARCHIVED   = '2'  // 归档稿件
ManuscriptStatus.RECYCLE    = '3'  // 回收站

// 素材类型
MaterialType.SHORT_PHRASE = '0'  // 短句
MaterialType.GOLDEN_QUOTE = '1'  // 金句
MaterialType.ALLUSION     = '2'  // 典故

// 自动保存开关
AutosaveSwitch.OFF = '0'
AutosaveSwitch.ON  = '1'

// 导出格式
ExportFormat.MD  = 'md'
ExportFormat.PDF = 'pdf'
```

### 2.4 DTO 路径参考

| 模块 | Request DTO | Response DTO |
|---|---|---|
| 文稿 | `manuscript/dto/requests/*` | `manuscript/dto/responses/*` |
| 素材 | `material/dto/requests/*` | `material/dto/responses/*` |
| 标签 | `tag/dto/requests/*` | `tag/dto/responses/*` |
| 设置 | `setting/dto/requests/*` | `setting/dto/responses/*` |

---

## 三、数据库表结构（Prisma）

### 3.1 表清单（5 张表）

| 表名 | 模型 | 说明 |
|---|---|---|
| `lit_manuscript` | `LitManuscript` | 文稿主表（status：0草稿/1正式/2归档/3回收站） |
| `lit_material` | `LitMaterial` | 素材表（type：0短句/1金句/2典故） |
| `lit_tag` | `LitTag` | 标签表（唯一约束：同一用户标签名不重复） |
| `lit_manuscript_tag` | `LitManuscriptTag` | 文稿标签关联（多对多，级联删除） |
| `lit_setting` | `LitSetting` | 用户编辑室设置（字体/自动保存/导出格式） |

### 3.2 关键字段说明

**LitManuscript**（`lit_manuscript`）

| 字段 | 类型 | 说明 |
|---|---|---|
| `manuscriptId` | Int | 主键，自增 |
| `tenantId` | VarChar(20) | 租户编号，默认 `000000` |
| `userId` | Int | 所属用户 |
| `title` | VarChar(200) | 标题 |
| `content` | Text | Markdown 内容 |
| `wordCount` | Int | 字数 |
| `status` | Char(1) | 状态（0/1/2/3） |
| `delFlag` | Char(1) | 删除标志（0正常/2已删除） |

**LitSetting**（`lit_setting`，唯一约束 `tenantId + userId`）

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `fontSize` | Int | 16 | 字体大小（px） |
| `fontFamily` | VarChar(100) | 空 | 字体族（CSS 值） |
| `autosave` | Char(1) | '1' | 自动保存开关 |
| `autosaveInterval` | Int | 30 | 自动保存间隔（秒） |
| `exportFormat` | Char(2) | 'md' | 默认导出格式 |

---

## 四、前端实现（Vue 3 + NaiveUI）

### 4.1 页面清单

| 页面 | 路径 | 文件 |
|---|---|---|
| 文稿工作台（首页） | `/literature/workbench` | `apps/web/src/views/literature/workbench/index.vue` |
| 我的稿件 | `/literature/manuscripts` | `apps/web/src/views/literature/manuscripts/index.vue` |
| 常用素材库 | `/literature/materials` | `apps/web/src/views/literature/materials/index.vue` |
| 标签管理 | `/literature/tags` | `apps/web/src/views/literature/tags/index.vue` |
| 编辑室设置 | `/literature/settings` | `apps/web/src/views/literature/settings/index.vue` |
| 打印预览 | `/literature/print/:id` | `apps/web/src/views/literature/print/[id].vue` |
| 文稿编辑器抽屉 | （共用组件） | `apps/web/src/views/literature/manuscripts/modules/manuscript-editor-drawer.vue` |

### 4.2 各页面功能明细

#### 文稿工作台（首页）
- 顶部统计卡片：草稿数、正式稿件数、归档稿件数、总字数、素材数、标签数
- 最近 10 篇文稿列表（可快速跳转编辑）
- 卡片悬浮上浮动效

#### 我的稿件
- NTabs 切换：草稿箱 / 正式稿件 / 归档稿件 / 回收站
- 关键词搜索 + 标签筛选
- 列表操作：编辑 / 复制 / 删除（回收站: 恢复 / 永久删除）
- 右侧抽屉式 Markdown 编辑器（`md-editor-v3`）

#### 常用素材库
- NTabs 切换：全部 / 短句 / 金句 / 典故
- 关键词搜索
- 列表操作：编辑 / 删除

#### 标签管理
- 关键词搜索
- 列表操作：编辑（NDrawer 弹窗）/ 管理 / 删除
- 标签颜色预览（NDrawer 内嵌 NColorPicker 色板）
- 每标签显示关联文稿数

#### 编辑室设置
- 字体大小滑块（12–24 px）
- 字体族单选（默认/Georgia/思源黑体/宋体/楷体）
- 自动保存开关（NSwitch）
- 自动保存间隔输入（5–300 秒）
- 默认导出格式单选（MD / PDF）
- 实时预览文字效果
- 存储空间概览统计

#### 打印预览 `/literature/print/:id`
- 调用 `MdPreview` 渲染 Markdown
- `window.print()` 直接打印，浏览器保存为 PDF
- 返回按钮

#### 文稿编辑器抽屉（共用组件）
- Markdown 编辑器 `md-editor-v3`（完整工具栏：粗体/斜体/删除线/标题/引用/列表/代码/链接/图片/表格等）
- 自动保存（setInterval，30s 节流）
- 手动保存按钮
- 图片粘贴上传 → `/literature/upload`（返回 URL）
- 支持亮色 / 暗色主题跟随全局

### 4.3 API 类型定义

全部类型定义在 `apps/web/src/typings/api/literature.api.d.ts`，位于 `Api.Literature` 命名空间下，包含：

| 类型 | 说明 |
|---|---|
| `Manuscript` | 文稿列表项 |
| `ManuscriptDetail` | 文稿详情（含内容与标签） |
| `ManuscriptList` | 文稿分页列表 |
| `Material` | 素材项 |
| `MaterialList` | 素材分页列表 |
| `Tag` | 标签项 |
| `TagList` | 标签分页列表 |
| `Setting` | 编辑室设置 |
| `WorkbenchOverview` | 工作台概览 |
| `SimpleList<T>` | 不分页列表（如最近文稿） |
| `ManuscriptSearchParams` 等 | 各接口查询/提交参数类型 |

### 4.4 API Service 层

文件 `apps/web/src/service/api/literature.ts`，共 23 个请求函数，全部通过统一 `request` 封装层发起。

### 4.5 Elegant Router 路由定义

```
/literature                    (目录)
├── /literature/workbench      → 文稿工作台（默认首页）
├── /literature/manuscripts    → 我的稿件
├── /literature/materials      → 常用素材库
├── /literature/tags           → 标签管理
├── /literature/settings       → 编辑室设置
└── /literature/print/:id      → 打印预览
```

### 4.6 国际化

文件 `apps/web/src/locales/langs/zh-cn.ts`，key 列表：

```
literature              文学编辑室
literature_workbench    文稿工作台
literature_manuscripts  我的稿件
literature_materials    常用素材库
literature_tags         标签管理
literature_settings     编辑室设置
```

### 4.7 seed.ts 菜单路由预置

`apps/server/prisma/seed.ts` 中预置了 7 条 `sys_menu` 记录（parent_id = 110），含完整 component 路径与 perms 权限标识，新库初始化后无需手动配置菜单即可在后台"系统管理"菜单中显示。

---

## 五、后续迭代建议

| 阶段 | 内容 |
|---|---|
| **二期** | 素材库：图片 / 音频上传；素材分组；素材引用统计 |
| **二期** | 文稿导出：MD / PDF 单篇导出；批量导出 |
| **三期** | 版本回溯：文稿历史记录 + 对比查看 + 回滚 |
| **三期** | 写作统计：日/周/月写作量图表；写作连续天数 |
| **待定** | 文稿分享链接（只读）；协作评论；AI 写作辅助 |

---

## 🚀 文学编辑室｜子路由模块实现文档（2026-08-09）

> **分支**: `feature/add-twomenu`
> **目标**: 逐个子路由模块（文稿工作台 / 我的稿件 / 常用素材库 / 标签管理 / 编辑室设置 / 打印预览）完整跑通基础写作闭环，并修复访问 `/literature/workbench` 时的页面崩溃。

### 0. 本次解决的问题（根因与修复）

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | 后端全部 literature 接口 500：`Cannot read properties of undefined (reading 'create'/'count'/'findMany')` | ① `PrismaService` 只为 `sys*`/`gen*` 等模型定义了显式 getter，**没有为 literature 的 5 个模型（litManuscript/litMaterial/litTag/litSetting/litManuscriptTag）定义 getter**；`BaseRepository` 通过 `prisma['litManuscript']` 解析委托 → undefined。② 运行中的后端加载的是旧 Prisma Client（未含 literature 模型） | ① 在 `prisma.service.ts` 补充 5 个模型 getter；② 重新生成 Prisma Client |
| 2 | 工作台 overview 500：`Invalid ... Unknown argument where` | `workbench.service.ts` 调用 `tagRepo.count({ where: {...} })` 多包了一层，基类 `count(where)` 期望裸 where | 改为 `tagRepo.count({ userId, delFlag: '0' })` |
| 3 | 前端访问 literature 页面崩溃：`Cannot read properties of null (reading 'parentNode'/'nextSibling')`（运行时 Vue `componentUpdateFn` → `hostParentNode(prevTree.el)`，prevTree.el 为 null） | 「我的稿件」页面的编辑器抽屉 `manuscript-editor-drawer.vue` 使用了 `defineAsyncComponent` 但**未从 vue 导入** → setup 阶段抛 `ReferenceError` → 该组件子树 el 为 null → 之后任何路由切换触发的渲染 flush 都会在 `parentNode(null)` 处崩溃，且会**波及后续所有页面导航** | 补上 `defineAsyncComponent` 导入 |
| 4 | 前端直接访问 `/literature/workbench` 控制台 `[Violation] Permissions policy violation: unload is not allowed` | Chromium 131+ 将 `unload` 加入 Permissions Policy 封锁，三个 store（app/tab/theme）用 `useEventListener(window, 'beforeunload', ...)` 注册页面卸载缓存，Chromium 触发页面卸载时跟进一次 `unload` 触发 Violation | app/tab/theme 三个 store 将 `beforeunload` 统一替换为 `pagehide`（Chromium 131+ 官方推荐的替代事件，不受该 policy 封锁，语义一致） |

> 💡 前端请求层约定：`@sa/axios` 的 `createFlatRequest` 返回 `{ data: <transformBackendResponse 结果>, error }`，分页接口转换后 `data = { rows, total, pageNum, pageSize, pages }`，因此页面统一用 `res.data?.rows` / `res.data?.total` 取值。

---

### 1. 文稿工作台（`/literature/workbench`）

- **后端**：`GET /literature/workbench/overview` → 返回 6 项统计（草稿/正式/归档/总字数/素材数/标签数）+ 最近 10 篇文稿。已修复并验证 200。
- **前端现状**：
  - ✅ 统计卡片、最近文稿表格、状态 Tag 渲染
  - ⬜ 「新建文稿」按钮为占位 `@click="() => {}"` → **待实现**：点击后跳转「我的稿件」并打开新建编辑器抽屉
  - ⬜ 最近文稿表格行不可点击 → **待实现**：点击行打开对应文稿编辑器
- **实现方案**：
  - 通过路由 query（如 `/literature/manuscripts?new=1`）或事件总线触达我的稿件页的编辑器抽屉；采用「我的稿件页暴露 `openEditor(id?)` 能力 + 工作台按钮跳转并带参数」的方式，保持组件解耦。

---

### 2. 我的稿件（`/literature/manuscripts`）

- **后端**：文稿 13 个接口（列表/最近/详情/新建/更新/自动保存/改状态/复制/绑标签/移回收站/恢复/永久删除）已全部验证 200。
- **前端现状**：
  - ✅ NTabs 四态切换（草稿箱/正式/归档/回收站）、搜索、标签筛选、列表操作（编辑/复制/删除/恢复/永久删除）
  - ✅ 右侧抽屉 Markdown 编辑器（`md-editor-v3`）+ 30s 自动保存 + 图片粘贴上传
  - ✅ `defineAsyncComponent` 崩溃已修复
  - ⬜ 编辑器抽屉内**标题保存**：目前 `flushSave` 只保存 content，标题未提交 → **待实现**：关闭/完成时若标题变化则调用 `fetchManuscriptUpdate` 保存标题
  - ⬜ 编辑器抽屉内**状态操作**（发布/归档）与**标签绑定** UI → **待实现**
- **实现方案**：
  - 编辑器抽屉增加「保存并发布 / 保存并归档」按钮，调用 `fetchManuscriptChangeStatus`
  - 抽屉内增加标签多选（NSelect 多选，来源 `fetchTagList`），保存时调用 `fetchManuscriptBindTags`
  - 关闭抽屉统一走 `flushSave`（含标题），避免只存正文不存标题

---

### 3. 常用素材库（`/literature/materials`）

- **后端**：素材 5 个接口（列表/详情/新建/更新/删除）已全部验证 200。
- **前端现状**：
  - ✅ NTabs 类型切换（全部/短句/金句/典故）、关键词搜索、列表、删除
  - ⬜ 「新增素材」按钮为占位 → **待实现**：新建抽屉（类型/内容/出处）
  - ⬜ 列表「编辑」按钮为占位 → **待实现**：编辑抽屉（回填 + 更新）
- **实现方案**：新增 `material-drawer.vue`（复用标签页抽屉模式），表单字段 = 类型（NSelect/NRadio）+ 内容（NInput textarea）+ 出处（NInput），提交走 `fetchMaterialCreate` / `fetchMaterialUpdate`。

---

### 4. 标签管理（`/literature/tags`）

- **后端**：标签 5 个接口（列表/详情/新建/更新/删除，列表含关联文稿数）已全部验证 200。
- **前端现状**：✅ 完整 CRUD（NDrawer 新增/编辑 + NColorPicker 颜色 + 关联文稿数展示），**无需再改**。

---

### 5. 编辑室设置（`/literature/settings`）

- **后端**：`GET/PUT /literature/setting`（首次访问自动建默认值）已验证 200。
- **前端现状**：
  - ✅ 字体大小滑块、字体族单选、自动保存开关、间隔、导出格式、实时预览、存储空间占位统计
  - ⬜ **Bug**：`label="字体大小（{{setting.fontSize}}px）"` 写在 HTML 属性里，`{{}}` 不参与插值，页面显示字面量「字体大小（{{sett…」 → **待实现**：改为 `:label` 绑定
  - ⬜ 存储空间统计为写死的 0 → **待实现**：接入工作台 overview 数据（文稿/素材/标签数量）
- **实现方案**：`label` 改 `:label="`字体大小（${setting.fontSize}px）`"`；存储空间卡片改为进入页面时拉取 `fetchWorkbenchOverview` 填充真实数量。

---

### 6. 打印预览（`/literature/print/:id`）

- **前端现状**：✅ `MdPreview` 渲染 + `window.print()` + 返回，**无需再改**。
- 后续可加：MD/PDF 单篇导出、批量导出（二期）。

---

### 7. 本次涉及的代码文件清单

| 文件 | 改动 |
|---|---|
| `apps/server/src/infrastructure/prisma/prisma.service.ts` | 补充 5 个 literature 模型 getter |
| `apps/server/src/module/literature/workbench/workbench.service.ts` | 修正 `tagRepo.count` 参数形状 |
| `apps/web/src/views/literature/manuscripts/modules/manuscript-editor-drawer.vue` | 补 `defineAsyncComponent` 导入（崩溃根因） |
| `apps/web/src/views/literature/settings/index.vue` | 修复 label 插值；接入真实存储统计 |
| `apps/web/src/views/literature/workbench/index.vue` | 新建文稿/最近文稿跳转 |
| `apps/web/src/views/literature/manuscripts/index.vue` + 编辑器抽屉 | 标题保存、状态操作、标签绑定 |
| `apps/web/src/views/literature/materials/index.vue` + 新增抽屉 | 新增/编辑素材 |

---

## Java正式环境部署
- [正式Java部署文档记录](./docs/deploy-online/nestjs-vue-production-deployment.md)

## 前端项目代理地址有一个坑
出了修改env配置文件，最后的build的配置文件也需要修改

- 前端使用npm安装，更加稳定。

## 图标库：
项目安装 unplugin-icons，自动按需加载 Google Material‑Symbols 图标，
配置文件：/Users/henryning/Documents/code/personCode/Nest-Admin-Soybean/apps/web/build/plugins/index.ts

这里使用的时候：直接传入，可以实现没有图标的自动在有网络的时候，自动下载：
这里图标


## 自动插件引入机制，使用自动
Fixed backend 404s caused by APP_PREFIX mismatch and redundant URI versioning. Added unplugin-vue-components with NaiveUiResolver for auto-importing naive-ui components. Restart the frontend dev server to verify the auto-import works.



## 后端所有的内容docs，使用AI实现
- apps/server/docs


## 统一去掉v1版本管理

保证所有前后端接口一致，未来需要再打开。

## 接口文档
当服务启动后，控制台会显示:
Swagger 文档: http://localhost:8080/api/swagger-ui/
服务地址: http://localhost:8080/api/
健康检查: http://localhost:8080/api/health


## 🗄️ Prisma 数据库初始化完整方案

> **更新时间**: 2026-08-05
> **状态**: ✅ 已完全解决
> **问题**: `sys_system_config` 表不存在导致接口 500 错误

### 📋 问题根源

**根本原因**: 数据库初始化存在**双重数据源冲突**

- `init.sql`（18个表）← Docker 自动执行的传统方式
- `Prisma Schema`（48个表）← 现代迁移系统
- **缺失 30 个表**，包括 `sys_system_config`、`sys_tenant_feature` 等

**导致问题**:
- ❌ 数据库结构与代码不同步
- ❌ 种子数据不完整（仅 17/48 个表有数据）
- ❌ 接口报错 500
- ❌ 迁移历史混乱

### 🔧 完整解决方案

#### 1. 数据库迁移重置

```bash
cd apps/server
rm -rf apps/server/prisma/migrations

pnpm run prisma:seed

```

- ✅ 成功应用 11 个迁移文件
- ✅ 创建所有 48 个数据库表
- ✅ 建立完整的迁移历史

#### 2. 种子数据补充

**核心表种子** (`prisma/seed.ts`):
- ✅ 18 个核心表数据（从 init.sql 生成）
- ✅ 包含 `sys_system_config`（6条记录）

**迁移表种子** (`prisma/migration-seed.ts`) ✨ 新增:
- ✅ `sys_system_config` - 系统配置（8条）
- ✅ `sys_tenant_feature` - 租户功能开关（5条）
- ✅ `sys_tenant_quota` - 租户配额（1条）
- ✅ `sys_oss_config` - OSS配置（1条）
- ✅ `sys_notify_template` - 站内信模板（3条）
- ✅ `gen_template_group` - 代码生成器模板组（1条）
- ✅ `sys_sms_channel` - 短信渠道（2条）
- ✅ `sys_mail_account` - 邮件账号（1条）

#### 3. Docker Compose 配置更新

**变更**: 移除 `init.sql` 自动初始化挂载

```yaml
# docker-compose-postgres-redis.yml
volumes:
  - postgres_data:/var/lib/postgresql/data
  # - ./apps/server/db:/docker-entrypoint-initdb.d  # ❌ 已移除
```

**原因**:
- init.sql 只包含 18 个表，与当前 48 个表的 Schema 不一致
- 与 Prisma 迁移系统冲突
- 新增的 30 个表无法通过 init.sql 初始化

#### 4. 新增工具和脚本

**种子覆盖率检查** (`scripts/check-seed-coverage.js`):
```bash
npm run prisma:check-seed
```

输出示例:
```
✅ 有数据:   26
⚠️  缺失:     8
❌ 必需缺失: 0
```

### 📚 完整文档导航

- **快速开始** → `prisma/SEED_SOLUTION.md`
- **完整指南** → `prisma/BEST_PRACTICES.md`
- **本次修复** → `prisma/FIX_SUMMARY.md`
- **问题分析** → `prisma/SOLUTION_SUMMARY.md`
- **Docker变更** → `prisma/docker-compose-change.md`
- **数据库初始化** → `prisma/db/README.md`

---
⚡ 关键命令速查

# 开发环境完整初始化
npm run prisma:init

# 补充迁移表种子
npm run prisma:seed:migration

# 检查覆盖率
npm run prisma:check-seed

# 生产环境部署
npm run prisma:deploy && npm run prisma:seed:migration

---

## 代码生成器动态路由缺失修复

> **更新时间**: 2026-08-08<br>
> **影响路由**: `tool_gen_datasource`、`tool_gen_template`、`tool_gen_history`

### 问题现象

`admin` 登录后，“系统工具 / 代码生成”下的数据源管理和模板管理没有显示。Vue 项目中的页面、Elegant Router 路由声明、懒加载映射和国际化文案均已存在，缺失发生在 NestJS 后端的动态菜单数据链路。

### 根本原因

1. `apps/server/prisma/seed.ts` 中没有预置数据源管理、模板管理和生成历史三条 `sys_menu` 记录。
2. `menu_id = 116` 的“代码生成”仍是菜单类型 `C`，但当前前端将它定义为包含三个页面的二级目录，因此应为目录类型 `M`。
3. 后端 `buildMenus()` 原本未在构建路由树前过滤按钮类型 `F`。当 116 改为目录后，其原有按钮权限会被误转换为 `#` 路由。

### 修改方案

| 文件 | 修改内容 |
|---|---|
| `apps/server/src/module/system/menu/utils.ts` | 构建路由树前过滤 `menuType = 'F'` 的按钮权限 |
| `apps/server/prisma/seed.ts` | 将 116 改为目录，并为新数据库预置 datasource/template/history 三条页面菜单及角色关联 |
| `apps/server/prisma/migration-seed.ts` | 为已有数据库增加幂等修复，按 component/path 定位菜单，不依赖固定自增 ID |
| `apps/server/test/unit/module/system/menu/menu.utils.spec.ts` | 覆盖三级菜单构建与按钮路由过滤的回归测试 |

### 现有数据库升级（推荐，不清空业务数据）

> 无需重新初始化整个数据库。`prisma:seed:migration` 可重复执行，会修正 116 并补齐缺失的子菜单。

```bash
cd apps/server
pnpm run prisma:seed:migration
```

如果当前环境的 pnpm 版本代理无法工作，可直接使用已安装的本地命令：

```bash
cd apps/server
./node_modules/.bin/ts-node prisma/migration-seed.ts
```

后端菜单接口使用 Redis 缓存，默认约 60–65 分钟过期。需要立即看到新菜单时，清理项目 Redis DB 2 缓存后重新登录：

```bash
cd apps/server
pnpm run redis:flush
```

> **注意**: `redis:flush` 会清空 Redis DB 2 的所有项目缓存，可能使当前会话失效；请勿在未确认影响范围时直接对生产环境执行。

### 全新开发库初始化（会删除全部现有数据）

仅在本地开发库可以丢弃全部数据时使用：

```bash
cd apps/server
pnpm run prisma:seed
```

`prisma:seed` 内部包含 `prisma db push --force-reset --accept-data-loss`，会重建数据库后依次执行 `prisma/seed.ts` 和 `prisma/migration-seed.ts`。

### 迁移故障处理

如果旧数据库执行菜单迁移时报错：

```text
P2002: Unique constraint failed on the fields: (`menu_id`)
```

原因是历史 seed 使用显式 `menu_id` 导入菜单，PostgreSQL 的 `sys_menu` 自增序列却未同步到表内的最大 ID。修复后的 `migration-seed.ts` 会在创建缺失菜单前自动执行等价的序列校准：

```sql
SELECT setval(
  pg_get_serial_sequence('sys_menu', 'menu_id'),
  COALESCE((SELECT MAX(menu_id) FROM sys_menu), 1),
  true
);
```

直接重新执行幂等迁移即可，不需要回滚或重置数据库：

```bash
cd apps/server
pnpm run prisma:seed:migration
```

### 数据验证

执行升级后，在 PostgreSQL 中查询：

```sql
SELECT menu_id, menu_name, parent_id, order_num, path, component, menu_type, visible, status, del_flag
FROM sys_menu
WHERE menu_id = 116 OR parent_id = 116
ORDER BY parent_id, order_num, menu_id;
```

预期结果：

- “代码生成”的 `menu_type = 'M'`、`component IS NULL`。
- 子菜单包含 `datasource`、`template`、`history`，三者的 `menu_type = 'C'`。
- 重新登录后，前端请求的 `/system/menu/getRouters` 返回的“代码生成” children 中包含对应三条路由，且不包含按钮权限生成的 `#` 路由。

---

### 📊 修复效果

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| **接口状态** | ❌ 500错误 | ✅ 200成功 |
| **sys_system_config** | ❌ 0条 | ✅ 8条 |
| **必需表完整率** | 94% | **100%** |
| **种子数据表** | 17个 | **26个** |
| **表总数** | 25个 | **48个** |
| **自动检查** | ❌ 无 | ✅ 有 |

### 🎯 新开发者设置

```bash
# 1. 启动 Docker 服务
docker-compose up -d postgres redis

# 2. 安装依赖
pnpm install

# 3. 初始化数据库（新步骤）
cd apps/server
npm run prisma:init

# ✅ 完成！数据库包含所有 48 个表
```

### 🔄 新增表的标准流程

```bash
# 1. 修改 schema.prisma
# 2. 创建迁移
npm run prisma:migrate -- --name add_new_table

# 3. 在 migration-seed.ts 添加种子数据
# 4. 仅运行迁移表种子
npm run prisma:seed:migration

# 5. 检查覆盖率
npm run prisma:check-seed

# 6. 提交代码
git commit -m "feat: add new_table with seed data"
```

### ⚠️ 重要变更

**Docker Compose 配置已更新**:
- ❌ 移除了 `./apps/server/db:/docker-entrypoint-initdb.d` 挂载
- ✅ 数据库完全由 Prisma 迁移系统管理
- ✅ 不再自动执行 `init.sql`
- ✅ `init.sql` 已备份到 `apps/server/db/backup/`

**影响**:
- ✅ 已拥有数据库的用户：无需操作
- ⚠️ 新克隆项目：需要运行 `npm run prisma:init`
- 📦 现有 `postgres_data` 卷不受影响

---
状态: ✅ 完全解决
接口: ✅ /api/v1/registerUser 正常返回 200
数据库: ✅ 49 个表（含迁移历史表）
种子数据: ✅ 26 个表有数据
文档: ✅ 完整（6份文档）
工具: ✅ 可用
验证: ✅ 通过
---



## 2026-08-05：前端架构优势梳理

### 1. 架构定位

当前前端属于面向企业后台的中台型架构。其主要优势不是单一框架或组件库，而是已经围绕权限、请求、状态、页面和基础能力形成稳定分层，适合持续扩展多角色、多菜单、多租户及重 CRUD 的管理模块。

```text
业务页面 views
   ↓
业务组件 / Hooks / Pinia Store
   ↓
领域 API（system、monitor、tool）
   ↓
统一 Request 治理
   ↓
NestJS 后端

路由权限、主题、国际化、工程化能力横向支撑各层
```

### 2. Monorepo 边界清晰

项目使用 pnpm workspace 管理 `apps/*`、`packages/*` 和 `apps/web/packages/*`，并通过 Turborepo 统一编排开发、构建、代码检查与类型检查任务。

前端内部进一步拆分了以下基础包：

- `@sa/axios`：请求基础封装
- `@sa/hooks`：通用组合式能力
- `@sa/utils`：通用工具函数
- `@sa/color`：颜色及主题相关能力
- `@sa/materials`：基础 UI 材料
- `@sa/uno-preset`：UnoCSS 预设
- `@sa/scripts`：工程脚本和代码生成工具
- `@nest-admin/types`：前后端可共享的类型定义

这种组织方式可以避免所有通用逻辑堆积在业务目录中，方便独立维护、复用和后续拆分子应用或组件库。

相关文件：

- `package.json`
- `pnpm-workspace.yaml`
- `turbo.json`
- `apps/web/package.json`
- `apps/web/packages/*`
- `packages/types`

### 3. 应用装配职责单一

`apps/web/src/main.ts` 仅负责依次装配 Loading、页面进度条、离线图标、Day.js、Pinia、Vue Router、国际化和版本更新检测，没有混入具体业务逻辑。

主要收益：

- 应用初始化顺序清晰
- 插件可以独立替换和测试
- 后续接入监控、埋点、微前端生命周期更加方便
- 避免入口文件随业务增长而不断膨胀

### 4. 权限与动态路由体系完整

路由层集中处理登录状态、常量路由、权限路由、角色授权、403/404 区分、登录后原路径回跳，以及静态路由和后端动态路由两种模式。

动态菜单、Vue Router 路由、面包屑和页面缓存都由同一套路由数据派生，能够降低菜单、路由和权限状态不一致的概率。

主要流程：

1. 初始化无需登录的常量路由。
2. 检查本地 Token 和用户信息。
3. 根据环境配置选择静态或动态权限路由。
4. 动态模式下从后端获取菜单路由。
5. 将路由转换并注册到 Vue Router。
6. 同步生成菜单、面包屑和 KeepAlive 缓存列表。

相关文件：

- `apps/web/src/router/guard/route.ts`
- `apps/web/src/store/modules/route/index.ts`
- `apps/web/src/store/modules/route/shared.ts`
- `apps/web/src/router/elegant`

### 5. 请求层集中治理

统一请求层负责处理：

- Token 和客户端 ID 注入
- 国际化请求头
- 防止短时间重复提交
- 请求参数加密
- Token 过期刷新与原请求重试
- 登录失效后的请求取消和统一退出
- 业务错误与 HTTP 错误提示
- Blob、ArrayBuffer 和分页数据转换

因此 `service/api` 中的业务接口只需要表达请求地址、方法、参数和返回类型。认证、安全策略或后端响应格式变化时，通常只需要调整请求层，不必逐个修改业务页面。

相关文件：

- `apps/web/src/service/request/index.ts`
- `apps/web/src/service/request/shared.ts`
- `apps/web/src/service/api`
- `apps/web/packages/axios`

### 6. 通用业务能力沉淀在 Hooks 中

项目将后台系统中高频出现的表格、分页、查询、表单、权限、字典、下载等行为封装为 Hooks，而不是在各页面重复实现。

例如 `useTable` 已统一处理：

- 请求状态与空数据状态
- 分页参数和序号计算
- 表格列显隐
- 移动端分页
- 横向滚动宽度
- 国际化切换后的列刷新

这种方式可以让新增 CRUD 页面主要关注业务字段和交互规则，同时保持不同业务模块的行为一致。

相关目录：

- `apps/web/src/hooks/common`
- `apps/web/src/hooks/business`
- `apps/web/packages/hooks`

### 7. 状态管理按领域拆分

Pinia Store 按职责拆分为 `auth`、`route`、`tab`、`theme`、`app`、`dict`、`notice` 等模块，而不是维护一个庞大的全局 Store。

主要优势：

- 状态所有权清晰
- 模块之间依赖关系容易追踪
- 登录退出时可以分模块清理状态
- 路由、标签页和缓存页面能够协同管理
- 新业务状态可以按领域独立扩展

相关目录：`apps/web/src/store/modules`。

### 8. 主题和后台体验能力完善

主题体系不仅支持亮色和暗色模式，还包括：

- 跟随系统主题
- 动态品牌色和 CSS Token
- 灰度模式和色弱模式
- 多种后台布局
- 用户名或时间水印
- Naive UI 主题覆盖
- 主题配置持久化

这套能力有利于白标化、客户定制和不同品牌环境下的快速交付。

相关文件：

- `apps/web/src/store/modules/theme/index.ts`
- `apps/web/src/store/modules/theme/shared.ts`
- `apps/web/src/theme`
- `apps/web/src/layouts`

### 9. 工程自动化和质量保障较完整

Vite 已集成 Vue、Vue JSX、Elegant Router、UnoCSS、本地图标、Vue DevTools 和构建进度插件。项目同时提供路由生成、类型检查、代码检查、提交校验、Vitest 单元测试以及 Cypress E2E 测试脚本。

本次验证结果：

```text
前端类型检查：通过
Vitest 测试文件：6 个全部通过
Vitest 测试用例：59 个全部通过
```

相关文件：

- `apps/web/build/plugins/index.ts`
- `apps/web/vitest.config.ts`
- `apps/web/cypress.config.ts`
- `apps/web/test`
- `apps/web/cypress/e2e`

### 10. 总结

当前前端架构的核心价值可以概括为：

1. **扩展快**：CRUD、权限、字典和表格等基础能力已经标准化。
2. **治理集中**：认证、错误处理、主题、路由和请求策略集中维护。
3. **边界清晰**：业务页面、状态、API、请求层和基础包职责明确。
4. **后台能力完整**：适合多角色、多菜单、多租户的企业管理系统。
5. **工程基线可靠**：具备类型检查、单元测试、E2E 测试和提交校验。

整体来看，这套架构非常适合继续建设企业级后台管理平台；其优势主要体现在业务模块能够快速复制扩展，同时仍保持统一的权限、安全、交互和工程规范。

---






## redis数据库相关


## 数据库迁移问题postgres
### 现在 PostgreSQL 连接应该可以正常工作了。如果需要迁移数据库，请运行：


cd apps/server
npx prisma migrate dev --name init




## 2026-08-05 

### 我看到这个错误是 Prisma 连接 PostgreSQL 时 TLS 握手失败。错误信息显示：Error opening a TLS connection: server does not support TLS

我看到问题了！在 .env.development 文件中，DATABASE_URL 没有 SSL 参数，但 DB_SSL=false 是单独设置的。这个错误说明 PostgreSQL 服务器不支持 TLS 连接。

问题在于 PrismaService.buildConnectionString() 方法可能会添加 SSL 参数。让我检查 AppConfigService 如何读取配置

找到问题了！.env.development 中的 DATABASE_URL 没有 SSL 参数，但 DB_SSL=false 是单独配置的。如果任何代码直接使用 DATABASE_URL 环境变量而不是通过 PrismaService 构建的连接字符串，就会出现 SSL 连接错误。


#### 每一次修改连接prisma后，需要删除缓存重新生成：
清除 Prisma 缓存并重新生成

rm -rf node_modules/.prisma node_modules/@prisma/client 2>&1 && npx prisma generate 2>&1 | tail -30

注意：&> ✅ 简写，等同于 2>&1





### prisma 错误问题说明：

修改文件位置  
@prisma/client 里面导出不到 SysPost / SysRole / SysUser / SysAuditLog
Prisma 没有生成最新客户端类型，schema 模型 和生成的 client 不同步。
几种情况：
schema.prisma 修改过模型，没有执行 prisma generate
node_modules/@prisma/client 是旧缓存
pnpm 模式下，prisma generate 输出位置异常
schema 里面模型名称和导入名称不一致（大小写、前缀 Sys_）

#### 解决方案
1. 执行 
```bash
pnpm --filter @nest-admin/server prisma generate
```
2. 检查 node_modules/@prisma/client
3. 重新生成 Prisma Client
```
# 1 删除旧生成产物
rm -rf node_modules/.prisma
rm -rf node_modules/@prisma/client

# 2 重新生成客户端
npx prisma generate
注意：pnpm技术栈：pnpm 项目推荐：
- pnpm prisma generate

3. 清除 TS 缓存（Nest 很关键）
rm -rf dist
rm -rf .nest


```



## 2026-08-02: Vite 启动失败 — build/ 目录缺失
修复方法：npm --filter @nest-admin/web build:dev

每次新拉取后，构建一下
### 问题描述

执行 `pnpm dev` 启动 @nest-admin/web 时，Vite 报错：

```
failed to load config from .../apps/web/vite.config.ts
Error: Build failed with 2 errors:
  vite.config.ts:4:33: ERROR: Could not resolve "./build/plugins"
  vite.config.ts:5:46: ERROR: Could not resolve "./build/config"
```

### 根因

`vite.config.ts` 在 commit `866475d` (Refactor:重构) 中被新增，文件中导入了 `./build/plugins` 和 `./build/config` 两个模块，但对应的 `build/` 目录从未被创建和提交到 git 仓库中。

git 历史记录显示 `apps/web/` 目录在此 commit 之前的快照中只有 `Dockerfile`、`package.json`、`src/` 等内容，`build/` 目录从未存在于任何提交中。

### 修复方案

创建 `apps/web/build/` 目录及以下文件：

#### build/plugins/index.ts

配置 Vite 插件，包含：
- `@vitejs/plugin-vue` — Vue SFC 编译支持
- `@vitejs/plugin-vue-jsx` — JSX/TSX 支持
- `@unocss/vite` — UnoCSS 原子化 CSS
- `vite-plugin-svg-icons` — SVG 图标精灵
- `vite-plugin-progress` — 构建进度条

**注意**：`vite-plugin-progress` 的导出为 default export `viteProgressBar`，不是命名导出 `vitePluginProgress`。误用会报错：

```
SyntaxError: The requested module 'vite-plugin-progress' does not provide an export named 'vitePluginProgress'
```

#### build/config/index.ts

包含两个工具函数：
- `createViteProxy(viteEnv, enable)` — 根据环境变量创建开发环境代理，转发 `/api` 和 `VITE_APP_BASE_API` 前缀到 `VITE_SERVICE_BASE_URL` 指定的后端地址
- `getBuildTime()` — 返回当前构建时间戳（依赖 `dayjs`）

### 涉及文件

| 文件 | 操作 |
|------|------|
| `apps/web/build/plugins/index.ts` | 新建 |
| `apps/web/build/config/index.ts` | 新建 |
| `apps/web/src/svg/` | 新建（`vite-plugin-svg-icons` 需要） |

## 2026-08-02: 路由视图缺失 — tool/build/index.vue 不存在

### 问题描述

Vite 启动后，依赖扫描阶段报错：

```
[plugin:vite:import-analysis] Failed to resolve import "@/views/tool/build/index.vue" from "src/router/elegant/imports.ts"
```

此文件由 `@elegant-router/vue` 自动生成的路由映射引入，路由定义为 `/tool/build`，对应功能为"表单构建"。

### 根因

`elegant-router` 生成的 `imports.ts` 中包含了 `tool_build` 路由的懒加载导入，但对应的视图文件 `src/views/tool/build/index.vue` 从未被创建。git 历史中也不存在此文件。

### 修复方案

创建 `src/views/tool/build/index.vue`，使用 `vform3-builds` (VForm 3) 表单设计器作为页面内容。`vform3-builds` 以 UMD 格式提供，通过动态加载脚本和样式文件的方式集成。

### 涉及文件

| 文件 | 操作 |
|------|------|
| `apps/web/src/views/tool/build/index.vue` | 新建 |





## 2026-08-05: Vite 代理配置错误导致 /auth/code 404

### 问题描述

前端访问 `/auth/code` 接口时出现 Vite 代理错误，后端返回 404：

```
[vite] http proxy error: auth/code
Cannot GET /api/auth/code
```

### 根因分析

**前端配置错误** (`apps/web/.env.dev`):
```bash
VITE_APP_BASE_API=/dev-api/v1/  # ❌ 错误：包含了 /v1/ 版本标识
```

**后端路由配置**:
- 全局前缀: `/api` (来自 APP_PREFIX)
- API 版本: `/v1` (通过 `app.enableVersioning()` 启用)
- 完整路由: `/api/v1/auth/code`

**Vite 代理重写规则** (`apps/web/build/config/index.ts`):
```typescript
rewrite: path => path.replace(new RegExp(`^${baseApi}`), '')
// 移除 /dev-api/v1/ 前缀，只发送 /auth/code 到后端
```

**错误的请求流程**:
```
前端: /dev-api/v1/auth/code
  ↓ Vite 代理移除 /dev-api/v1/
后端: /auth/code  ❌ 404 (期望 /api/v1/auth/code)
```

### 修复方案

修改前端环境变量，移除版本标识，让后端统一管理 API 版本：

```bash
# apps/web/.env.dev
VITE_APP_BASE_API=/dev-api/  # ✅ 正确：不包含版本号
```

**修复后的请求流程**:
```
前端: /dev-api/v1/auth/code
  ↓ Vite 代理移除 /dev-api/
后端: /api/v1/auth/code  ✅ 200 成功
```

### 验证结果

| 接口 | 路径 | 状态 |
|------|------|------|
| 注册用户 | `/api/v1/registerUser` | ✅ 200 |
| 获取验证码 | `/api/v1/auth/code` | ✅ 200 |
| 错误路径 | `/api/auth/code` | ❌ 404 |

### 影响范围

- **开发环境**: 需要重启 Vite dev server 以加载新的环境变量
- **测试环境**: `.env.test` 配置正确 (`/test-api`)，无需修改
- **生产环境**: `.env.prod` 配置正确 (`/prod-api`)，无需修改

### 关键要点

1. **API 版本控制应该由后端统一管理**
2. **前端环境变量 `VITE_APP_BASE_API` 不应包含版本号** (`/v1/`, `/v2/` 等)
3. **Vite 代理的职责**: 仅移除前端自定义前缀 (`/dev-api/`)，保留后端完整路由 (`/api/v1/`)

### 相关文件

| 文件 | 变更 |
|------|------|
| `apps/web/.env.dev` | 修改 `VITE_APP_BASE_API=/dev-api/v1/` → `/dev-api/` |
| `apps/web/.env.test` | ✅ 已正确 (`/test-api`) |
| `apps/web/.env.prod` | ✅ 已正确 (`/prod-api`) |

---

## 数据库默认预置账号及配置说明

### 1. 预置账号表 (`apps/server/db/init.sql` & `apps/server/prisma/seed.ts`)

| 账号类型 | 用户名 (`userName`) | 密码 | 昵称 (`nickName`) | 租户编号 (`tenantId`) | 部门 (`deptId`) | 账号描述 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **超级管理员** | `admin` | `admin123` / `123456` | Nest Admin | `000000` | 103 (研发部门) | 系统超级管理员，拥有完整权限 |
| **测试人员** | `test` | `admin123` / `123456` | Nest Admin Test | `000000` | 105 (测试部门) | 测试业务账号 |
| **演示测试账号** | `demo` | `demo123` | 演示账号 | `000000` | 103 (研发部门) | 前端演示一键登录默认账号 |

### 2. 系统核心初始配置

在 `sys_config` 与 `sys_tenant` 表中内置数据：
* **默认超级管理员租户**：`000000`（公司名称：`管理组`）
* **系统默认初始化密码**：`123456`（配置项 `sys.user.initPassword`）
* **账号验证码开关**：`true`（配置项 `sys.account.captchaEnabled`）
* **用户注册功能开关**：`false`（配置项 `sys.account.registerUser`）

---

## 前端路由初始化流程

> **更新时间**: 2026-08-06

### 路由守卫入口

`apps/web/src/router/guard/route.ts` — `router.beforeEach` 每次路由跳转时调用 `initRoute(to)`。

```
用户访问页面
     │
     ▼
router.beforeEach (路由守卫)
     │
     ▼
initRoute(to)  ← 核心初始化函数
     │
     ├── 常驻路由未初始化?
     │     yes → routeStore.initConstantRoute()
     │              ├── createStaticRoutes() → 获取常驻路由
     │              ├── addConstantRoutes()  → 存储
     │              └── handleConstantAndAuthRoutes()
     │                    ├── 合并常驻+鉴权路由
     │                    ├── sortRoutesByOrder() 排序
     │                    ├── getAuthVueRoutes() → 转为 Vue Router 格式
     │                    ├── addRoutesToVueRouter() → 动态添加到 router
     │                    ├── getGlobalMenus() → 生成菜单
     │                    └── getCacheRoutes() → 获取缓存列表
     │
     │             返回 → 重定向到原路径（让初始化后的路由生效）
     │
     ├── 用户未登录?
     │     yes → 常驻路由? → 放行 / 否则跳转登录页
     │
     ├── 鉴权路由未初始化?
     │     yes → routeStore.initAuthRoute()
     │              ├── 先初始化用户信息 (authStore.initUserInfo())
     │              │
     │              ├── static 模式:
     │              │     createStaticRoutes().authRoutes
     │              │     → 按角色过滤 (filterAuthRoutesByRoles)
     │              │
     │              └── dynamic 模式:
     │                    fetchMenuGetRouters()  → 从后端获取路由
     │                    → addAuthRoutes() 转换并存储
     │                    → handleUpdateRootRouteRedirect()
     │
     │             返回 → 重定向到原路径
     │
     └── 正常放行 → handleRouteSwitch()
```

### initRoute() 函数说明

| 步骤 | 条件 | 操作 |
|---|---|---|
| 1 | `!isInitConstantRoute` | 初始化常驻路由（登录页、404等），然后重定向让新路由生效 |
| 2 | 未登录 + 常驻路由 | 放行（如访问登录页） |
| 3 | 未登录 + 非 | 跳转到登录页 |
| 4 | 登录 + `!isInitAuthRoute` | 初始化鉴权路由，从后端获取动态路由，然后重定向 |
| 5 | 已初始化 | 正常放行 |

### 核心处理函数

`apps/web/src/store/modules/route/index.ts` — `handleConstantAndAuthRoutes()`

```ts
function handleConstantAndAuthRoutes() {
  const allRoutes = [...constantRoutes, ...authRoutes];  // 合并路由
  const sortRoutes = sortRoutesByOrder(allRoutes);        // 排序
  const vueRoutes = getAuthVueRoutes(sortRoutes);         // 转 Vue Router 格式
  resetVueRoutes();                                       // 清除旧路由
  addRoutesToVueRouter(vueRoutes);                        // 动态添加到 router
  getGlobalMenus(sortRoutes);                             // 生成菜单
  getCacheRoutes(vueRoutes);                              // 获取缓存列表
}
```

### 两种路由模式对比

| 模式 | 常驻路由来源 | 鉴权路由来源 | 适用场景 |
|---|---|---|---|
| `static` | `createStaticRoutes()` → `generatedRoutes`（文件系统自动扫描） | `createStaticRoutes().authRoutes`（按角色过滤） | 开发环境，路由由文件系统自动生成 |
| `dynamic` | `dynamicConstantRoutes`（手动定义） | `fetchMenuGetRouters()` → 后端返回 | 生产环境，路由由后端动态管理 |

### 关键文件

| 文件 | 职责 |
|---|---|
| `apps/web/src/router/guard/route.ts` | 路由守卫，控制初始化流程和访问权限 |
| `apps/web/src/store/modules/route/index.ts` | 路由状态管理，初始化、转换、菜单生成 |
| `apps/web/src/router/routes/index.ts` | 路由定义，常驻路由和两种模式的路由创建函数 |
| `apps/web/src/router/elegant/transform.ts` | Elegant 路由格式 → Vue Router 格式转换 |
| `apps/web/src/service/api/auth/index.ts` | 后端 API 调用（动态路由获取） |

---

## 2026-08-09：文学编辑室子路由模块实现与问题修复

> **分支**: `feature/add-twomenu`
> **目标**: 逐个子路由模块完整跑通基础写作闭环，并修复访问 `/literature/workbench` 时的页面崩溃

---

### 一、本次修改文件总表

#### 后端（4 个文件）

| 文件 | 改动内容 |
|---|---|
| `apps/server/src/infrastructure/prisma/prisma.service.ts` | 补充 5 个 literature 模型 getter（`litManuscript/litMaterial/litTag/litSetting/litManuscriptTag`） |
| `apps/server/src/module/literature/workbench/workbench.service.ts` | 修正 `tagRepo.count` 调用参数（去掉多余 `where` 嵌套） |
| `apps/server/src/module/literature/material/dto/requests/index.ts` | `type` 字段增加 `@Transform`，空字符串 → `undefined`，避免 `@IsIn` 校验 400 |
| `apps/server/prisma/schema.prisma` | literature 5 张表结构（`lit_manuscript`/`lit_material`/`lit_tag`/`lit_manuscript_tag`/`lit_setting`），已有，本次修 bug |

#### 前端（9 个文件）

| 文件 | 改动内容 |
|---|---|
| `apps/web/src/views/literature/manuscripts/modules/manuscript-editor-drawer.vue` | **彻底重写**：补 `defineAsyncComponent` 导入；支持新建（create 记录）；标题保存；发布/归档状态操作；标签多选绑定；30s 自动保存 |
| `apps/web/src/views/literature/manuscripts/index.vue` | 新增 `useRoute`/`useRouter`；支持 `?action=new/edit&id=` query 跳转打开编辑器 |
| `apps/web/src/views/literature/workbench/index.vue` | 新建文稿按钮→跳转我的稿件；最近文稿行→点击进入编辑 |
| `apps/web/src/views/literature/materials/index.vue` | 编辑按钮可点击；新增素材打开抽屉；`loadData` 修"全部" tab 不传空 `type` |
| `apps/web/src/views/literature/materials/modules/material-drawer.vue` | **新建**：新增/编辑素材抽屉（类型+内容+出处，NRadioGroup+NInput+NSelect 回填） |
| `apps/web/src/views/literature/settings/index.vue` | label `{{}}` 插值修复；存储空间接入 `fetchWorkbenchOverview` 真实数据 |
| `apps/web/src/store/modules/app/index.ts` | `beforeunload` → `pagehide` |
| `apps/web/src/store/modules/tab/index.ts` | `beforeunload` → `pagehide` |
| `apps/web/src/store/modules/theme/index.ts` | `beforeunload` → `pagehide` |

---

### 二、遇到的问题与根因

#### 问题 1｜后端全部 literature 接口 500

**现象**：
```
Cannot read properties of undefined (reading 'create'/'count'/'findMany')
```

**根因（双重）**：
1. `PrismaService` 只为 `sys*`/`gen*` 等传统模型定义了显式 getter，**literature 的 5 个模型（`litManuscript`/`litMaterial`/`litTag`/`litSetting`/`litManuscriptTag`）没有定义 getter**。`BaseRepository` 构造时通过 `prisma['litManuscript']` 动态解析委托 → `undefined`。
2. 运行中的后端加载的是旧版 Prisma Client（生成时间早于 literature 模型加入 schema），客户端不含 `litManuscript` 等模型。

**修复**：
- `prisma.service.ts` 补充 5 个 getter
- `rm -rf node_modules/.prisma node_modules/@prisma/client && npx prisma generate` 重新生成客户端
- NestJS `nest start --watch` 自动重编译后生效

**验证**：`curl` 逐一测试 26 个 literature 接口，全部返回 200。

---

#### 问题 2｜工作台 overview 500：`Invalid ... Unknown argument where`

**现象**：
```
Invalid `this.delegate.count()` invocation
Unknown argument `where`
```

**根因**：`workbench.service.ts` 调用 `tagRepo.count({ where: { userId, delFlag: '0' } })`，但 `SoftDeleteRepository.count()` 的签名是 `count(where?: Record<string, unknown>)`，传参形状为 `{ where: {...} }`，导致 Prisma 收到 `{ where: { where: {...} } }` 嵌套报错。

**修复**：改为 `tagRepo.count({ userId, delFlag: '0' })`。

---

#### 问题 3｜前端页面崩溃：`Cannot read properties of null (reading 'parentNode')`

**现象**：
```
TypeError: Cannot read properties of null (reading 'parentNode')
  at parentNode (runtime-dom.esm-bundler.js:51)
  at ReactiveEffect.componentUpdateFn
  at updateComponent
```

**根因**：「我的稿件」页面的编辑器抽屉 `manuscript-editor-drawer.vue` 第 26 行使用了 `defineAsyncComponent()`，但该函数**未从 `vue` 包导入** → Vue 3 setup 阶段抛出 `ReferenceError: defineAsyncComponent is not defined` → 该组件渲染失败，Vue 将其 VNode el 设为 `null` → 之后**任何路由切换**触发的渲染 flush 里，Vue 内部调用 `hostParentNode(prevTree.el)` 时 `prevTree.el === null`，进而调用 `.parentNode` 崩 → 并且这个崩溃是 `Uncaught (in promise)`，在全局范围被吞，导致**所有后续页面导航都被波及**。

**修复**：补 `defineAsyncComponent` 导入（`import { defineAsyncComponent, ... } from 'vue'`）。

**验证**：无头浏览器逐页访问 5 个 literature 页面，修复后全部零错误。

---

#### 问题 4｜素材库列表空显示 + `Uncaught (in promise) AxiosError`

**现象**：访问素材库显示"暂无素材"，控制台有 `AxiosError`。

**根因**：复现时后端处于 500 状态（问题 1 未修），请求失败；修复后同一个请求（`type=` 空字符串）触发 `@IsIn(['0','1','2'])` 校验失败，返回 400。

**修复**：
- 前端：`activeTab === 'all'` 时不传 `type` 参数
- 后端 DTO：`@Transform(({ value }) => value === '' ? undefined : value)` 容忍空字符串

---

#### 问题 5｜设置页 label 不插值

**现象**：`<NFormItem label="字体大小（{{setting.fontSize}}px）">` 在 HTML 属性内，`{{}}` 不参与插值，页面显示字面量 `字体大小（{{sett…`。

**修复**：改为 `:label="`字体大小（${setting.fontSize}px）`"` 动态绑定。

---

#### 问题 6｜`[Violation] Permissions policy violation: unload is not allowed`

**现象**：直接访问 `/literature/workbench`（未登录）时控制台出现该 Violation。

**根因**：Chromium 131+ 将 `unload` 事件加入 Permissions Policy 封锁。未登录时路由守卫会触发页面卸载 → 跳登录页，三个 store（`app`/`tab`/`theme`）用 `beforeunload` 做页面卸载缓存 → Chromium 跟进触发 `unload` 被策略层拦截告警。

**修复**：三个 store 的 `beforeunload` 统一替换为 `pagehide`（Chromium 131+ 官方推荐替代事件，语义等价，不受该 policy 封锁）。

---

### 三、后端接口验证记录（全部通过 200）

```
GET    /literature/workbench/overview          ✅ 200
POST   /literature/manuscript                   ✅ 200（创建 + 详情 + 列表 + 复制 + 恢复）
PUT    /literature/manuscript                   ✅ 200（更新 + 自动保存）
PUT    /literature/manuscript/status             ✅ 200（发布 + 归档）
PUT    /literature/manuscript/tags               ✅ 200（绑定标签）
DELETE /literature/manuscript/:id                ✅ 200（移入回收站）
DELETE /literature/manuscript/permanent/:id      ✅ 200（永久删除）
POST   /literature/material                      ✅ 200（创建 + 详情 + 列表）
PUT    /literature/material                      ✅ 200（更新）
DELETE /literature/material/:id                  ✅ 200（删除）
POST   /literature/tag                           ✅ 200（创建 + 详情 + 列表）
PUT    /literature/tag                           ✅ 200（更新）
DELETE /literature/tag/:id                       ✅ 200（删除）
GET    /literature/setting                       ✅ 200（首次访问自动创建默认值）
PUT    /literature/setting                       ✅ 200（更新）
POST   /literature/upload                        ✅ 200（图片上传）
```

---

### 四、前端页面验证记录（无头浏览器 CDP 逐页访问）

| 页面 | URL | 渲染状态 | 控制台错误 |
|---|---|---|---|
| 文稿工作台 | `/literature/workbench` | ✅ 统计卡片+最近文稿表格正常 | 0 |
| 我的稿件 | `/literature/manuscripts` | ✅ Tabs 四态+搜索+标签筛选+操作按钮 | 0 |
| 常用素材库 | `/literature/materials` | ✅ Tabs 类型切换+搜索+列表+新增/编辑抽屉 | 0 |
| 标签管理 | `/literature/tags` | ✅ 搜索+列表+NDrawer 新增/编辑 | 0 |
| 编辑室设置 | `/literature/settings` | ✅ 字体滑块+字体族+自动保存+预览+真实存储统计 | 0 |
| 打印预览 | `/literature/print/:id` | ✅ MdPreview 渲染+打印按钮 | 0 |

---

### 五、遗留说明

#### components.d.ts 预存类型错误（34 个，非本次引入）

`apps/web/components.d.ts` 由 `unplugin-vue-components` 自动生成，包含一批无效图标条目（`hugeicons`/`material-symbols` 等未安装的包），导致 `vue-tsc` 报语法错误。这是 c5a3349 提交时就存在的问题，**与本次改动无关**，但会阻塞 `typecheck`/`build`。建议后续单独清理。

#### 编辑器抽屉标题保存逻辑

当前 `flushSave` 在关闭抽屉/切换 tab 时触发，会调用 `fetchManuscriptUpdate` 保存标题+内容。注意：自动保存的 `setInterval` 只存 `content`（轻量），标题仅在关闭或手动点"完成"时一并提交。

#### 测试数据

调试期间在 dev 库通过 API 创建了 1 篇文稿（"测试文稿一"，status=正式）、1 个素材、1 个标签，可在 `my稿件→正式稿件` 或 `素材库` 看到。

---
## 2026-08-09（补充）：Markdown 编辑器布局 & 三模式切换实现

> **分支**: `feature/add-twomenu`
> **文件**: `apps/web/src/views/literature/manuscripts/modules/manuscript-editor-drawer.vue`
> **需求**：工具栏水平横向排布；编辑区占满抽屉剩余全部高度；支持 编辑 / 即时渲染预览 / 分屏 三种模式

---

### 一、布局结构（严格三层）

```
抽屉 n-drawer-content-wrapper  (display:flex; flex-direction:column; height:100%)
│
├── .editor-container  (display:flex; flex-direction:column; height:100%; gap:10px)
│   ├── .toolbar-row     ← 标题 + 标签多选 + 保存/发布/归档/完成（flex-shrink:0）
│   ├── .mode-bar        ← 视图切换：✏️ 编辑 / 👁️ 预览 / ↔️ 分屏（flex-shrink:0）
│   └── .editor-main     ← NCard 编辑主体（flex:1; height:0; min-height:0）
│       └── NSpin → MdEditor / MdPreview（flex:1 占满）
```

### 二、关键坑与修复（务必记录，后续接手可直接参考）

| # | 坑 | 原因 | 修复 |
|---|---|---|---|
| 1 | 工具栏纵向排列 | `md-editor-v3/lib/style.css` **未在编辑器组件导入**（仅 print 页导入过，样式"碰运气"生效） | 组件内 `import 'md-editor-v3/lib/style.css'` |
| 2 | 编辑区高度为 0（`editorMainH: 0`） | 用 `NSpace` 包裹三块布局，NSpace 会给每个子元素外包 `.n-space-item`，导致 `flex:1` 作用在错误元素上 | **改用纯 `<div class="editor-container">`**，直接子元素参与 flex |
| 3 | NSpin 内 md-editor 不占满 | NSpin 的 `.n-spin-container`/`.n-spin-content` 默认只占内容高度，未拉伸 | 设 `height:100%; display:flex; flex-direction:column` |
| 4 | `n-drawer-content-wrapper` 不参与 flex | NDrawer 是 **portal 渲染在 body 下**，无 scoped `data-v` 属性，scoped 的 `:deep()` 匹配不到 | 用 `:global(.n-drawer-content-wrapper)` 全局样式 |
| 5 | `inst.previewOnly is not a function` | 误以为 preview/previewOnly 是 MdEditor 实例方法；实际 `expose` 只暴露 `getSelectedText/focus/resetHistory/getEditorView` | 改用 prop/组件方案 |
| 6 | `:preview-only` prop 不生效 | md-editor-v3 内部 `previewOnly` 状态**硬编码初始化为 false**，不从 prop 读取（仅工具栏内部 toggle 控制）；而 `:preview` prop 生效 | **纯预览模式改用独立 `MdPreview` 组件**（`v-if` 切换） |

### 三、三模式实现方案（最终版）

```vue
<!-- 编辑区模板 -->
<MdPreview v-if="editorMode === 'preview'" :modelValue="manuscript.content" ... />
<MdEditor v-else :key="editorMode" v-model="manuscript.content" :preview="editorMode === 'split'" ... />
```

| 模式 | 渲染组件 | 关键 props | 效果 |
|---|---|---|---|
| ✏️ 编辑 | `MdEditor` | `:preview="false"` | 仅源码编辑面板 |
| 👁️ 预览 | `MdPreview` | `:modelValue="content"` | 仅渲染预览页面 |
| ↔️ 分屏 | `MdEditor` | `:preview="true"` + `:key="split"` 重挂载 | 编辑+预览双栏并排 |

> `:key="editorMode"` 强制重挂载，保证 `:preview` 初始值与内部状态一致。

### 四、验证结果（无头浏览器实测）

- 工具栏：19 个按钮 `top` 全部相同、`left` 递增 → **横向排列确认**
- 编辑区：`editorMainH = mdEditorH = 337px`（= 抽屉 469 - 工具栏 76 - 模式栏 36 - gap 20）→ **占满剩余高度确认**
- 分屏：`edW=503 + pvW=503` 双栏并排
- 预览：`hasEditor:false`（编辑区消失）+ MdPreview 渲染
- 全程控制台 **0 错误**

---
